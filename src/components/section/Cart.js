import React, { Component } from 'react';
import CartIndex from '../CartIndex';

class Cart extends Component {
    render() {
        return (
            <div>
                <CartIndex />
            </div>
        );
    }
}

export default Cart;